// Side-effect import. Pulls the variable font files into the bundle via
// Vite + fontsource. Kept isolated so main.jsx doesn't have to know the
// details. Import once from main.jsx.

import '@fontsource-variable/space-grotesk';
import '@fontsource-variable/inter';
import '@fontsource-variable/jetbrains-mono';
